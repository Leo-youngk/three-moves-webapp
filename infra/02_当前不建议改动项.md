# 02 当前不建议改动项

下面这些内容在后续中国可用版迁移中，除非有明确替换方案，否则暂时不要改。

## 不建议现在改的内容
- 页面层文案
- 页面结构
- 路由结构
- 本地数据模型
- 视觉风格
- 安装页内容逻辑
- 提醒页的产品层交互

## 不建议现在改的原因
- 这些内容已经验证过可用
- 迁移阶段最容易引入回归的就是重写 UI
- 基础设施问题应由基础设施层解决，不应通过改产品层绕开

## 如果未来必须改
- 先在 `infra/` 或 `deploy/` 里记录原因
- 再改最小范围的代码
- 不要同时改 UI、路由、数据模型和部署方式